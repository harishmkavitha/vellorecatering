RESPONSIVE MEDIA FIX PATCH

Copy the contents of this patch into the root of your GitHub project and allow replacement/merge.
Then commit and push.

This patch fixes the <picture> hero wrapper so images fill the full hero height on mobile/tablet/desktop and adds a dedicated Wedding tablet hero.
